#pragma once

#include <vector>

namespace Engine {
// Arena Allocator:
// Simple implementation of an arena allocator.
// Can be used to allocate objects of any type into a contiguous memory
// buffer.
/*
template <size_t ByteSize> class ArenaAllocator {
  private:
    std::vector<uint8_t> data;

  public:
    ArenaAllocator();
}
*/

} // namespace Engine